import React from 'react';

import { Nav } from './styles';

const Header = () => (
  <Nav>
    <a href="">Home</a>
    <a href="">About</a>
    <a href="">Sign in</a>
    <a href="">Login</a>
  </Nav>
);

export default Header;
